ul.comment-list
  - post.comments.includes(:user).each do | comment |
    li.comment
      .comment-body
        h3
          = comment.user.name
        .meta
          = comment.created_at.strftime('%B %d, %Y AT %H:%M%p')
        p
          = comment.content